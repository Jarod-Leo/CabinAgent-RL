# NTU EEE GPU Cluster 02 详细操作指南

本文是面向用户和 AI 代理的中文操作指南，适用于在 NTU EEE GPU Cluster 02 上配置环境、调试和运行实验。它不能替代官方文档；集群实时配置和管理员通知拥有最高优先级。

## 官方资料

- AI 代理摘要：https://github.com/NTUEEECluster/docs/blob/main/agent.md
- 使用规范：https://github.com/NTUEEECluster/docs/blob/main/guideline.md
- 集群、资源和存储：https://github.com/NTUEEECluster/docs/blob/main/cluster.md
- Slurm 使用方法：https://github.com/NTUEEECluster/docs/blob/main/slurm.md
- 配额与计费：https://github.com/NTUEEECluster/docs/blob/main/slurm-accounting.md
- 故障排查：https://github.com/NTUEEECluster/docs/blob/main/troubleshooting.md

每次开始新的集群任务时，应重新查看官方 AI 代理摘要。不同页面中的静态配额表可能更新不同步，因此 GPU 数量、QoS、账户和存储限额必须以 `sacctmgr`、`scontrol`、`sinfo`、`sshare` 和 `storagemgr` 的实时结果为准。

## 一、登录节点只能用于管理工作

登录节点用于编辑代码、轻量环境配置、提交任务和查看状态，不能用于训练、推理、数据预处理、评测、大规模编译、大文件下载、递归扫描或其他重负载工作。

- 登录节点没有 GPU。
- 每位用户在登录节点上有 16 GB 内存上限和 CPU 限制。
- 任意一个进程使用户内存超过上限时，该用户在该登录节点上的所有进程都可能被一起终止。
- SSH 断开后，登录节点上的进程会被清理，包括通过 `tmux` 或 `nohup` 启动的进程。
- AI 代理、代码索引器、文件监视器和语言服务器应尽量运行在本地电脑，只通过 SSH 执行范围明确的远程操作。

任何非轻量计算，即使不需要 GPU，也应通过 Slurm 提交。

## 二、SSH 连接规则

- 必须连接 NTUSECURE 或 NTU VPN。
- 使用入口地址登录，例如：`ssh <用户名>@10.97.216.128`。
- 入口地址会自动把会话分配到 `login-1`、`login-2` 或 `login-3`。
- 登录节点之间禁止互相 SSH；不能通过 `ssh login-2` 切换节点。如确实需要重新分配，应完全退出后重新连接。
- 只有当用户在某个计算节点上存在正在运行的 Slurm 任务时，才允许连接该计算节点。
- 连接已分配的计算节点通常使用：`ssh -J <用户名>@<入口地址> <用户名>@<计算节点>`。
- 没有有效任务时，应使用 `sinfo`、`squeue`、`scontrol` 或 `sacct` 查看状态，不能直接 SSH 到 GPU 节点。

## 三、所有计算必须通过 Slurm

正式实验优先使用 `sbatch`。`srun` 和 `salloc` 只适合短时间调试。

- 交互任务最多 2 小时、1 块 GPU，同时只能运行 1 个交互任务。
- 交互任务会在网络或 SSH 断开时取消，因此不能用于长时间训练。
- 批处理任务最长运行 3 天。
- 不得用空闲 shell 占用 GPU；任务不再使用资源时必须立即退出或取消。
- 较小、约束较少的任务通常更快获得资源。

常用命令：

```bash
sbatch <任务脚本>
squeue --me
sinfo
scontrol show job <任务编号>
sacct -j <任务编号>
scancel <任务编号>
```

## 四、资源申请规则

GPU 任务必须明确指定 GPU 型号或合法约束：

```bash
--gpus a40:1
-C gpu_48g
```

必须遵守以下规则：

- 不指定 GPU 型号或约束的 GPU 请求会被阻止。
- GPU 任务不要设置 `--mem` 或 `--cpus-per-task`；集群会根据 GPU 数量和型号自动分配 CPU 与内存，并覆盖这些参数。
- 一个任务不能跨多个物理节点组合 GPU。提交前必须确认单个节点能够满足 GPU 型号和数量要求。
- 需要较多内存的 `pro6000` 任务可在确认实时配置后考虑 `-C highmem`，但这会缩小可调度节点范围。
- 多卡任务排队时间通常显著更长。提交多卡或长任务前，必须先完成短时间、单卡冒烟测试。
- 不得请求实验不需要的稀缺 GPU 或长运行时间。

提交前检查实时账户和上限：

```bash
sacctmgr show assoc where user=$USER format=Account,QOS,DefaultQOS
sacctmgr show qos -P format=Name,MaxTRESPerUser
sinfo
squeue --me
```

## 五、存储规则

- `/home/$USER` 通常为 50 GB，只放代码、配置和少量 IDE 状态。
- `/tmp` 通常为每位用户 4 GB，只用于小型临时文件。
- 数据集、Conda 环境、模型权重、检查点、缓存、日志和实验输出应放在项目 SSD。
- 普通用户通过登录节点上的 `storagemgr` 创建和管理 `/projects/<项目名>`。
- Faculty-project 用户使用管理员提供的 `/projects/faculty/<项目名>`，不得使用 `storagemgr`。
- HDD 仅用于冷数据、大型归档或顺序读取的大文件；训练数据、Conda 环境和大量小文件必须放 SSD。
- 项目目录创建后不得改名。
- 大规模下载或实验前检查剩余配额；必要时把 `TMPDIR`、pip、Conda 和模型缓存重定向到项目 SSD。
- 集群不负责备份。代码、重要检查点和最终结果必须另行备份。

不得使用 `chmod -R 777` 等全员开放权限。定期用 `ls -la` 检查权限，只授予完成协作所需的最小访问权限。

## 六、软件和环境管理

集群使用 Lmod 管理编译器、CUDA 和常用软件：

```bash
module avail
module spider <软件名>
module show <软件名>
module load <软件名>/<版本>
module list
module purge
```

Python 包必须安装在个人命名的 Conda 环境中：

```bash
module load Miniforge3
source activate
conda create -n <环境名> python=3.10
source activate <环境名>
pip install <软件包>
```

- 不要同时加载 Miniforge3 和 Miniconda3。
- 基础环境只读，不要尝试向其中安装软件包。
- 不得使用 `sudo` 或修改系统软件包。
- 为保证可复现性，应尽量固定模块、Python 和依赖版本，并保存环境信息。

## 七、配额与 QoS

普通学生账户通常按月计算 GPU 服务单位，Faculty-project 用户则使用项目共享配额。具体规则和数值可能变化，提交长任务或参数扫描前必须查看实时状态。

```bash
sacctmgr show assoc user=$USER
sshare -U
```

`override-limits-but-killable` 是只使用空闲 GPU 的可抢占 QoS：

- 任务可能随时被重新排队。
- 只有用户确实拥有该 QoS 时才能使用。
- 使用前必须确认程序能够频繁保存检查点并自动恢复。
- 它不能用于长期占用或预留资源。
- CPU-only 任务不消耗 GPU 服务单位，适合可在 CPU 上完成的数据准备或轻量评测。

Faculty-project 任务必须按照管理员提供的信息同时指定正确的账户和项目 QoS，避免从错误配额扣费。

## 八、实验提交前检查

执行轻量只读检查：

```bash
pwd
git status --short
sacctmgr show assoc where user=$USER format=Account,QOS,DefaultQOS
sacctmgr show qos -P format=Name,MaxTRESPerUser
sinfo
squeue --me
```

提交前必须确认：

- 工作目录、数据集、环境、检查点、缓存和日志均位于正确的项目 SSD。
- 已核实账户、QoS、GPU 型号、GPU 数量、运行时间和当前配额。
- 已保存代码提交编号、配置、随机种子、模块版本和依赖版本。
- 输出目录不会覆盖其他实验，日志文件最好包含 Slurm 任务编号。
- 长任务和可抢占任务能够保存检查点并自动恢复。
- 单卡短时间冒烟测试已经通过。

## 九、推荐的批处理脚本

```bash
#!/usr/bin/env bash
#SBATCH --job-name=<实验名称>
#SBATCH --gpus=<GPU型号>:1
#SBATCH --time=01:00:00
#SBATCH --output=/projects/<项目名>/logs/%x-%j.out
#SBATCH --error=/projects/<项目名>/logs/%x-%j.err

set -euo pipefail
cd /projects/<项目名>/<代码目录>
module purge
module load Miniforge3
source activate <环境名>
python <入口脚本.py> <参数>
```

GPU 任务中不要添加 `--mem` 或 `--cpus-per-task`。默认情况下使用账户的默认 QoS；只有在实时账户信息和用户意图明确支持时，才能指定其他 QoS。

## 十、提交后的管理

- 记录 `sbatch` 返回的任务编号。
- 使用 `squeue --me` 查看排队或运行状态，用 `scontrol show job <任务编号>` 查看详细原因。
- Slurm 不会在提交时复制代码。任务处于排队状态时，不得修改它将读取的脚本或代码。
- 如需修改，先用 `scancel` 取消旧任务，建立新的固定代码版本后重新提交。
- 如果长期排队，先查看原因；在科学目标允许时，优先减少 GPU 数量或放宽 GPU 型号约束。
- 对已经失败、无效或空闲的任务应立即取消。
- 重试前保存日志，并记录任务编号、节点、命令、环境和完整错误信息，避免无依据地重复提交。

## 十一、安全、删除与管理员支持

- 密码、API 密钥、访问令牌和私钥不能写入仓库、任务脚本、命令参数、Shell 历史或日志。
- 不得共享账户，也不得尝试列出、读取或修改其他用户的 home/project 目录；相关行为会被记录。
- 删除文件、批量取消任务、递归修改权限或重建环境之前，必须显示准确目标并获得用户明确同意。
- 禁止对 `/home`、`/projects`、`/tmp`、`$HOME`、未解析变量或通配符执行宽泛的递归删除或权限修改。
- 集群只允许用于获批的 NTU 研究和项目计算，禁止挖矿、商业任务、恶意程序、漏洞利用、盗版软件和个人聊天机器人服务。

出现问题时先阅读官方故障排查文档。管理员主要处理集群基础设施问题，不负责普通用户代码错误。确需联系管理员时，应提供任务编号、时间、节点、复现步骤、日志、模块/环境版本和已经完成的检查，绝不能附带密码或密钥。

## 模块加载
- 先检查可用模块；不要假设系统已安装软件：

```bash
module avail
module spider <package>
```

- 每个 job 开始时清理并加载所需模块：
```bash
module purge
module load Miniforge3
module load CUDA
```

- 激活已有 Conda 环境：
```bash
source activate <env_name>
```
- 首次创建环境

```bash
module load Miniforge3
source activate
conda create -n <env_name> python=3.10
source activate <env_name>
```
- 不要尝试使用 sudo 或在系统路径安装 CUDA/Conda；Python 依赖安装在自己的 Conda 环境中。
- GPU/CUDA 操作只能在 Slurm 分配的计算节点内运行，不能在 login node 上跑训练。