# 02 集群运行时与双模型部署阶段

## 状态

已完成。

## 目标

- 在项目 SSD 中建立可复现的 CUDA/Conda/veRL/vLLM 环境。
- 部署 Qwen2.5-72B-AWQ user simulator 和 Qwen2.5-7B policy。
- 验证同一物理节点双 GPU 原子申请、服务互联和缓存位置。

## 设置

| 角色 | 设置 |
|---|---|
| Simulator | `Qwen/Qwen2.5-72B-Instruct-AWQ`，vLLM，AWQ-Marlin，TP=1 |
| Policy | `Qwen/Qwen2.5-7B-Instruct`，vLLM/veRL |
| 拓扑 | 同一台 highmem Pro 6000 物理节点，共 2 GPU；一个 srun step 内两个 task 各 1 GPU |
| Simulator context | `8192` |
| Simulator batching | `max_num_seqs=16`，`max_num_batched_tokens=32768` |
| Policy context | prompt `24576` + response `8192` = `32768` |
| 缓存 | vLLM、Triton、TorchInductor、HF、Ray 和临时缓存全部定向项目 SSD |
| 调度 | `cluster02`，account/QoS `msc`，`--nodes=1 --gres=gpu:pro6000:2`，`--constraint=highmem` |

## 执行结果

- 环境作业 `131248` 完成，耗时 `20:45`。
- 模型下载作业 `131280` 完成，7B 和 72B-AWQ 均标记 ready。
- CAR parquet 作业 `131281` 完成：103 train、26 dev、20 gate。
- 优化后的 simulator smoke `131323` 在 `gpu-pro6000-7` 完成，耗时 `3:50`。
- vLLM 确认 AWQ-Marlin 可用；启动分析显示约 39.7 GiB KV cache 和 15.88x 的 8192-token concurrency 估计。

## 已完成改进

- Hugging Face 下载支持 429/5xx/超时有界重试、`Retry-After` 和断点续传。
- 所有 shell 入口在 Slurm 中显式使用 `bash`，避免 Windows 同步丢失 executable bit。
- Conda 激活改为 `conda shell.bash hook` 后显式 `conda activate`，避免继承 submit shell 状态。
- 72B kernel 从 generic AWQ 改为实测支持的 AWQ-Marlin。
- 7B context 从非法 40960 修正为模型允许的 32768，并加入五组配置回归测试。

## 遗留问题与下一步

- 运行时和双模型部署已满足后续实验要求。
- 模型服务的成功不代表训练链路成功；后续仍需通过 rollout gate 和真实 veRL trainer smoke。

## 主要产物

- `configs/runtime/pro6000_dual_node.yaml`
- `configs/serve/simulator_vllm.yaml`
- `scripts/cluster_runtime_env.sh`
- `scripts/serve_simulator_vllm.sh`
- `scripts/serve_policy_vllm.sh`

### 2026-09-01 单节点双 GPU 拓扑改进

#### 实验设置

- G03 与正式 GRPO 均固定为 `--nodes=1 --gres=gpu:pro6000:2 --constraint=highmem`。
- 一个 srun step 使用 `--ntasks=2 --gpus-per-task=1 --gpu-bind=single:1`；task 0 运行 simulator，task 1 运行 policy/trainer，通过 `127.0.0.1` 通信。
- 每个作业保存 Slurm stdout/stderr，以及 allocation、simulator、policy/trainer、rollout/gate-check 独立日志。

#### 执行结果

- 实时 QoS 确认 `msc` 允许每用户最多 2 块 Pro 6000；集群存在单节点 4/8/10 卡 Pro 6000 节点，可满足同节点双卡申请。
- 2026-09-01 检查时用户队列为空，highmem Pro 6000 均已分配，因此新作业预计先进入排队。
- 本地静态回归测试已确认两个双 GPU sbatch 均为单节点申请，尚待远端 `bash -n` 与 `sbatch --test-only`。

#### 改进原因

- 集群规则明确禁止一个任务跨多个物理节点组合 GPU；旧 G00-G03 脚本的 `--nodes=2` 拓扑不再允许继续使用。
- 分组件日志可以区分资源绑定、模型服务、rollout、gate-check 和 trainer 故障。

#### 改进措施

- 将 gate 和 GRPO sbatch 改为单节点双 GPU，并删除 hostname 角色拆分。
- 新增 allocation 资源审计日志和 Job ID 命名的组件日志。
- 提交前必须在集群执行 shell syntax、配置断言和 Slurm test-only；实际运行后核对 allocation 日志中的单一节点和两块 GPU。
- 正式 G03 前先执行 10 分钟上限的同节点双 GPU 绑定 smoke；两个并发 step 的 `SLURM_STEP_GPUS` 必须非空且互不相同，G03 通过 `afterok` 依赖该 smoke。

### 同节点双 GPU smoke attempt 1 / Slurm 132933

#### 实验设置

- `--nodes=1 --gres=gpu:pro6000:2 --constraint=highmem`，两个后台 `srun` 各申请 1 块 Pro 6000。
- 保存 Slurm stdout/stderr、allocation 和两个 step 日志，并比较 `SLURM_STEP_GPUS`。

#### 执行结果

- Slurm 在同一物理节点 `gpu-pro6000-7` 分配了两块 GPU，Job GRES 为物理索引 `0,4`。
- 两个 step 均以 `0:0` 完成，但都记录 `SLURM_STEP_GPUS=0` 和同一 UUID；batch 校验因此正确返回失败，作业 `132933` 最终状态 `FAILED`，耗时 1 秒。

#### 改进原因

- Step 只执行一次 `nvidia-smi` 就退出，两个后台请求没有形成稳定的并发重叠；第二个 step 在第一块 GPU 被释放后复用了 GPU 0，无法验证长驻双服务的并行绑定。

#### 改进措施

- 每个 step 在记录绑定后保持 10 秒，使两个 GPU allocation 同时存活，再比较 `SLURM_STEP_GPUS` 和 UUID。
- 使用新 Job ID 提交 attempt 2；旧 132933 日志完整保留。G03 必须依赖新的 smoke PASS，不能依赖 132933。

### 同节点双 GPU smoke attempt 2 / Slurm 132950

#### 实验设置

- 在 attempt 1 基础上让两个独立 one-GPU `srun --exclusive` step 各保持 10 秒，尝试形成稳定并发。

#### 执行结果

- 作业在 `gpu-pro6000-7` 获得两块 GPU，但 step `132950.0` 与 `132950.1` 仍串行运行，各 10 秒；batch 最终 FAIL。

#### 改进原因

- 集群为每个 exclusive step 继承了作业级内存资源，导致第二个 step 等待第一个释放，无法同时启动 simulator 和 policy。

#### 改进措施

- 放弃两个独立 srun step，改为一个 srun step 内启动两个 task：`--ntasks=2 --gpus-per-task=1 --gpu-bind=single:1`。
- 新 smoke 直接比较两个 task 的 GPU UUID；G03 使用相同绑定模型并通过 task role 分工。

### 同节点双 GPU smoke attempt 3 / Slurm 132966

#### 实验设置

- 单一 srun step、两个 task，`--gpus-per-task=1 --gpu-bind=single:1`；每个 task 记录 hostname、CUDA 可见设备和 GPU UUID。

#### 执行结果

- 作业 `132966` 在 `gpu-pro6000-3` 完成，耗时 10 秒，退出码 `0:0`。
- Task 0/1 分别获得 UUID `GPU-bd67f840-...` 与 `GPU-d9b859d6-...`，同节点且 GPU 不同，输出 `SAME_NODE_DUAL_GPU_OK`。

#### 改进原因

- Attempt 1/2 证明独立 exclusive step 会串行复用 GPU，必须在同一个 step 内完成并发 task 绑定。

#### 改进措施

- 将单-step双-task绑定设为 G03 和后续 GRPO 的唯一合法启动模型。
- G03 allocation 与 role 日志必须核对 `NumNodes=1`、`gres/gpu:pro6000=2` 和两个 role 正常启动。

### HDD direct-load smoke preparation / 2026-09-04

#### 实验设置

- 保持 72B-AWQ 的 vLLM、AWQ-Marlin、context、batching 与采样 smoke 设置不变，仅允许 `slurm_simulator_smoke.sbatch` 通过环境变量显式选择模型目录。
- 计划在 copy-only 归档 PASS 后，分别从 HDD 读取 corrected-F01 merged 7B parent 与 72B-AWQ simulator；启动阈值约 20 分钟，均使用单卡 Pro 6000 短作业。

#### 执行结果

- 本地新增静态回归并通过 41 项测试、compileall 和 diff check；HDD 归档仍在运行，尚未提交 GPU load smoke。

#### 改进原因

- 原 simulator smoke 无条件覆盖 `SIMULATOR_MODEL_PATH` 为 SSD 路径，无法验证已确认的“不可变模型直接从 HDD 加载”策略。

#### 改进措施

- 归档完成并记录后同步这一单行兼容改动，执行远端 Bash/test-only；7B 与 72B 均能从 HDD 完成加载/生成且启动时间不超过约 20 分钟，才允许把对应 SSD 模型列入待删除清单。

### HDD corrected-F01 parent load smoke attempt 1 / Slurm 138060

#### 实验设置

- 输入为已通过归档 hash gate 的 HDD corrected-F01 merged parent；运行完整 parent manifest path/size/SHA-256 校验、BF16 Transformers GPU load、tokenizer 与 one-token generation。
- Normal QoS、1 node、1x Pro 6000 highmem、4 个调度器强制 CPU、90 GiB node memory、1 小时；不修改模型或训练状态。

#### 执行结果

- 同步包 hash、远端 41 tests、Bash syntax 与 exact HDD path Slurm test-only 通过。
- Job `138060` 于 2026-09-03 17:21:05--17:22:51 UTC 在单卡 Pro 6000 上完成，Slurm `COMPLETED/0:0`，elapsed `00:01:46`，stderr 为空。
- HDD parent 的 10 个 manifest 文件、`15,242,726,337` bytes 全量 hash PASS；BF16 加载 `7,615,616,512` 参数，tokenizer size `151,665`，one-token generation PASS。

#### 改进原因

- 精确归档只证明字节一致，还需验证计算节点能在可接受时间内直接从 HDD 加载并执行模型，才能把 SSD parent 判为可删除冷副本。

#### 改进措施

- 上述 PASS 条件全部满足，实际总用时远低于约 20 分钟阈值。完成本 attempt 记录同步后允许提交 72B-AWQ HDD simulator smoke；SSD 仍不删除。

### HDD 72B-AWQ simulator load smoke attempt 1 / Slurm 138064

#### 实验设置

- Simulator 模型指向 verified HDD archive；保持 AWQ-Marlin、BF16/auto dtype、TP=1、8K context、16 sequences、32K batched tokens、prefix caching、chunked prefill 与 0.92 smoke memory utilization。
- Normal QoS、1 node、1x Pro 6000 highmem、4 个调度器强制 CPU、90 GiB node memory、30 分钟；启动 vLLM health 后运行既有 simulator request smoke。

#### 执行结果

- 配置改动同步 hash、远端 41 tests、Bash syntax 与 exact HDD model path Slurm test-only 均通过。
- Job `138064` 于 2026-09-03 17:25:35--17:30:12 UTC 在单卡 Pro 6000 上完成，Slurm `COMPLETED/0:0`，elapsed `00:04:37`。
- HDD 的 11 个 safetensors shard 在 `122.16s` 读完，模型载入共 `128.29s`、占用 `38.77 GiB`；AWQ-Marlin、FlashAttention2、compile、health 均成功。
- Base、hallucination 与 disambiguation 三类 simulator 请求全部 HTTP 200，JSON report 含 3 条结果。启动前 health curl connection-refused 重试与服务正常退出时的 NCCL destroy warning 不影响结果。

#### 改进原因

- 72B-AWQ 是训练期间 simulator 的最大不可变冷源；必须证明 HDD 启动与实际请求正常，才能删除约 41.61 GB SSD 副本并让后续 F10 直接使用 HDD。

#### 改进措施

- 上述 PASS 条件全部满足，总用时 `4m37s`，远低于约 20 分钟阈值。7B/72B 直接从 HDD 加载的前置门禁闭合。
- 下一步向用户提供四个已验证 HDD 副本对应的精确 SSD 删除清单；只有再次明确确认后才提交安全删除 Job。F10 continuous run 仍未提交。
